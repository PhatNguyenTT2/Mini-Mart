# Stage 1E — R2-ER1-G1 fresh central decision

Verdict: `NO_SELECTION_EVIDENCE_REMAINS_INSUFFICIENT`
Runtime: `Sol Max Fast` (`gpt-5.6-sol`, reasoning `max`, service tier `priority`)
Execution authority: `DENIED`

## Outcome

The change-controlled remediation round is mechanically valid, but it does not make LightGCN selectable. All three independent evidence lanes remain `EVIDENCE_INCOMPLETE`; the positive gate therefore has `0/3` sufficient lanes and selects zero candidates.

The schema findings from the previous R2 round are closed only through immutable normalization sidecars. The sidecars preserve all 21 original row statuses and do not create scientific evidence.

## Evidence gained

- The SIGIR paper relationship, exact PyTorch repository revision, repository command and major configuration fields are now tightly bound.
- The LightGCN Gowalla processed files are tied to the corresponding NGCF repository objects by immutable object identity.
- The exact README result surface and same-revision evaluator behavior are localized without joining them to the distinct paper result/configuration surface.

## Blocking evidence

1. **Code use:** no affirmative license grant is established for the repository-owned Python, C++ or documentation at the pinned revision.
2. **Dataset:** the canonical SNAP family is identified, but the exact lawful raw release and raw-to-filter-to-ID-map-to-split/validation lineage are not reproducibly bound.
3. **Result provenance:** no immutable producing run, selected checkpoint, evaluator receipt, run aggregation/variation or deterministic tie contract is available.
4. **Comparability:** the paper center, repository README center and future harmonized-v5 evaluator are separate surfaces and cannot be silently cross-joined.

Central replay attempted ten decision-bearing authoritative locators: seven succeeded and three GitHub API routes failed in the central web client. Those failures were not used as positive evidence and cannot promote an incomplete lane.

## Pipeline consequence

`R2-M0` and every materialization/execution stage remain blocked. No repository, source archive, dataset, checkpoint or asset was downloaded; no package/environment was installed; no preprocessing, training, evaluation or TEST access occurred.

The empirical truth remains:

- `RESULT_STATUS=NOT_RUN`;
- `TEST_SET_OPENED=NO`;
- `ACCEPTED_RESULT_ROWS=0`;
- project benchmark numbers remain `INVALID_FOR_PAPER`.

## Required user decision

The current public authoritative evidence route is exhausted for a positive LightGCN selection. The next step must be one explicit scope decision:

1. authorize a maintainer-contact plan for license/provenance clarification;
2. create a new, separately labeled framework/dataset reproduction row with affirmative licenses and traceable lineage; or
3. pause official reproduction and continue only paper work that does not depend on comparative benchmark numbers.

There is no automatic fallback. A framework reproduction must never be relabeled as official reproduction of the LightGCN paper center.
