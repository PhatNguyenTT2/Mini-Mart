# Chatbot maintenance tools

These utilities are operational maintenance tools, not ML benchmark lineage
generators. They are intentionally kept outside `seed-product/` so the
benchmark run surface remains small and auditable.
