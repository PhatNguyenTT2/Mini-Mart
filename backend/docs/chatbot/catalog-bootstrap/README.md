# Catalog bootstrap tools

These scripts are separate catalog/customer bootstrap utilities. They are not
part of the immutable ML benchmark pipeline under `seed-product/` and must not
be run during an R3 or production training campaign.

The active benchmark seed is documented in
`seed-product/benchmark-spec-v4.json` and uses only the scripts listed there.
